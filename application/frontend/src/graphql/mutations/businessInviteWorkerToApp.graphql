mutation businessInviteWorkerToApp ($i: InviteWorkerInput!){
    businessInviteWorkerToApp(input: $i){
        id
        first_name
        last_name
        email
        position{
        id
        name
        }
    }
}
