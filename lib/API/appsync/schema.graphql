input DemoInput {
  version: String!
}

type Mutation {
  addDemo(input: DemoInput!): demo
}

type Query {
  getDemos: [demo!]
  callLambda(id: String!): String
}

type demo {
  id: String!
  version: String!
}
